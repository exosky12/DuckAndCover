<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DuckAndCoverApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DuckAndCoverApp"
    Shell.FlyoutBehavior="Flyout"
    Title="DuckAndCoverApp">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />
    <ShellContent
        Title="Crédits"
        ContentTemplate="{DataTemplate local:CreditPage}"
        Route="CreditPage" />
    <ShellContent
        Title="Regle"
        ContentTemplate="{DataTemplate local:Regle}"
        Route="ReglePage" />

</Shell>
